<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cubiculo</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/materia/bootstrap.min.css">
    <!-- <script>
      function verFormulario() {
        //alert("no hay cubiculo");
        if (data == 0){
          alert("Dato incorrecto");
        }
      }
    </script> -->
</head>

<body>
    <!-- barra de navegacion -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarColor01">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <a class="navbar-brand" href="/" style=" margin-right:5px; margin-left:5px;"> < </a>
            </li>
            <li class="nav-item">
              <a class="navbar-brand" href="/" align="left" style=" margin-right:5px; margin-left:5px;"> Cubiculos </a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <br>

<!-- donde ocurre la magia -->
<table class="table table-bordered table-hover" align="center" border="1" style="width:1000px; height:50px; margin-top:5px;">
    <thead></thead>
    <tbody>
          <tr>
            <td><form action="/cubiculo/add" method="POST">
                <div class="col-sm-10">
                    <label for="exampleInputPhone" class="form-label mt-4">Numero</label>
                    <input type="text" name="Numero" placeholder="Numero" class="form-control" autofocus>
                  </div>
                <div class="form-group">
                    <label for="exampleTextarea" class="form-label mt-4">Dotacion</label>
                    <input type="text" name="Dotacion" placeholder="Dotacion" class="form-control" autofocus>
                  </div>
                  <div class="form-group">
                    <label for="exampleSelect1" class="form-label mt-4">Estado</label>
                    <input type="text" name="Estado" placeholder="Disponible/NoDisponible" class="form-control" autofocus>
                  </div>
                  <br>
                <button type="submit" class="btn btn-primary">
                  Guardar cubiculo
                </button>
              </form></td>
          </tr>
    </tbody>
  </table>

  <table class="table table-bordered table-hover" align="center" border="1" style="width:1000px; height:50px;">
    <thead>
      <tr>
        <td>n°</td>
        <td>Numero</td>
        <td>Dotacion</td>
        <td>Estado</td>
        <td>Actions</td>
      </tr>
    </thead>
    <tbody>
      <% if (data) { %>
        <% for(var i = 0; i < data.length; i++) { %>
          <tr>
            <td><%= (i + 1) %></td>
            <td><%= data[i].Numero %></td>
            <td><%= data[i].Dotacion %></td>
            <td><%= data[i].Estado %></td>
            <td>
              <a href="/cubiculo/edit/<%= data[i].Numero %>" class="btn btn-info">Editar</a>
              <a href="/cubiculo/delete/<%= data[i].Numero %>" class="btn btn-danger">Borrar</a>
            </td>
          </tr>
          <% } %>
      <% } %>
    </tbody>
  </table>
</body>
</html>